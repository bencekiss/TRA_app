<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Memini</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://fonts.googleapis.com/css?family=Titillium+Web" rel="stylesheet">
  </head>

  <body>
    <header>
      <nav class="navbar">
        <ul class="navlist">
          <% if current_user %>
            <li class="navelement"><%= link_to image_tag('logo', id: "logo"), account_path(current_user), class: "head-link" %></li>
          <% else %>
            <li class="navelement"><%= link_to image_tag('logo', id: "logo"), root_path, class: "head-link"%></li>
          <% end %>
          <% if current_user %>
            <li class="navelement"><%= link_to "My Account", account_path(current_user.id) %></li>
            <li class="navelement"><%= link_to "Sign Out", :logout, method: :post %></li>
          <% else %>
            <li class="navelement"><%= link_to "Log In", new_account_session_path %></li>
            <li class="navelement"><%= link_to "Sign Up", new_account_path %></li>
          <% end %>
        </ul>
      </nav>
    </header>
    <main>
      <%= yield %>
    </main>
  </body>
</html>
